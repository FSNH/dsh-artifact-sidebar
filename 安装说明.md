# 会话产物侧栏插件 — 安装说明

> 插件版本：`pkg-24`（最终定型） · 来源：DeepSeek Harness 动态 Cordis 插件 `artf-2`

---

## 1. 包内文件

| 文件 | 说明 |
| --- | --- |
| `plugin.host.js` | Host 半部（数据收集 RPC：会话产物、文件读取、打开文件夹） |
| `plugin.client.js` | Client 半部（UI：右上角开关、侧栏面板、预览、全屏、复制路径） |
| `会话产物侧栏-用户使用指南.md` | 功能使用说明 |
| `安装说明.md` | 本文件 |

---

## 2. 安装方式（在对方的 DSH 会话中）

### 方式 A：由 Agent 一键复现（推荐）

对方在 DSH Web GUI 打开任意会话（建议使用带 Cordis 工具集的预设，如 `cordis`），然后**把这两个文件发给你的 Agent**（或让 Agent 读取文件路径），要求：

1. 调用 `cordis_define`：
   - `plugin.idPrefix`：`artf`
   - `name`：`会话产物侧栏`
   - `purpose`：一句话描述
   - `code.host` = `plugin.host.js` 的内容（即函数体）
   - `code.client` = `plugin.client.js` 的内容（即函数体）
2. 调用 `cordis_run` 激活（界面批准后生效）。

Agent 若能看到这两个文件，直接说「读取 `plugin.host.js` 和 `plugin.client.js`，用 cordis_define 定义会话产物侧栏插件并运行」即可。

### 方式 B：手动复制代码

把两个文件的内容分别粘贴到 `cordis_define` 的 `code.host` / `code.client` 字段，然后 `cordis_run`。

---

## 3. 注意事项

| 事项 | 说明 |
| --- | --- |
| **会话级** | 动态插件只存在于当前会话进程，**重启后消失**，需要重新定义 |
| **批准** | 首次运行需在界面点击「批准」；勾选信任未来版本可免后续批准 |
| **依赖服务** | Host 需要 `workspaceRegistry` / `sessionQuery` / `fs` / `subprocess`（标准 DSH 部署均自带） |
| **平台** | 「在文件夹中打开」使用 Windows `explorer.exe /select`（Windows 专属；其他平台需改 `artifacts/reveal` 处理器） |
| **产物来源** | 从会话日志的 `write`/`edit` 工具调用提取；仅用 bash 直接创建的文件需先经 write 工具写入才会出现 |

---

## 4. 想做成「永久生效」怎么办

动态插件是进程内的。若要在**每次启动自动加载**，需要把这段代码打包成该部署的 **agent preset**（一个 Cordis 组合目录，含本地插件文件），放到对方部署的 `${DSH_HOME:-$HOME/.dsh}/.agent-presets/<id>/` 下，并在会话中选择该预设。

> 注意：预设组合中的本地插件运行在常规 Cordis 环境，本插件的 `harness`/`host`/`slots` 等动态内置只在**动态插件运行环境**可用，因此**推荐做法仍是「会话内 cordis_define」**；如需常驻，请让对方部署的维护者评估将该代码注册为官方 client 插件的方案。

---

## 5. 验证清单（安装后）

1. 会话右上角出现圆形「面板」图标 ▣；
2. 点击展开 → 会话区收窄，显示**当前会话**的产物文件；
3. 悬停文件 → 📖 预览 / ⧉ 复制路径 / ↗ 打开文件夹；
4. 📖 预览 → 只显示内容，头部 ✕ 返回列表；
5. 切换会话 → 侧栏自动跟随显示新会话产物。

---

*分享包生成于 DSH 会话（artf-2/pkg-23），配套文档：`会话产物侧栏-用户使用指南.md`。*
