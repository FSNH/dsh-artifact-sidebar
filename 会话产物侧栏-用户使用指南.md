# 会话产物侧栏 — 用户使用指南

> 插件：`artf-2`（会话产物侧栏） · 当前版本：`pkg-23` · 运行状态：✅ 运行中（已定型）
> 适用界面：DeepSeek Harness Web GUI

---

## 1. 插件能做什么

在页面右侧提供**可展开/收起的产物侧栏**，用于查看**当前会话**在对话过程中**实际产生（写入）的文件**：

- 一个会话对应一份产物清单，**不同会话显示不同文件**，互不混叠；
- 侧栏**嵌入页面布局**：展开时会话区自动收窄，收起时恢复原始宽度；
- 支持文件**预览（全屏内容模式）、复制路径、在文件夹中打开、全屏**等快捷操作。

---

## 2. 快速上手（30 秒）

```
1. 打开任意会话，进入对话
2. 点击会话右上角的圆形「面板」图标 ▣  ← 展开侧栏
3. 侧栏展开，会话区自动收窄，显示该会话的产物文件
4. 点文件行的 📖 进入预览（只显示内容）；点头部 ✕ 返回列表
5. 点侧栏头部最右侧的 ▣ 图标收起，会话区恢复原宽
```

---

## 3. 界面元素详解

### 3.1 展开/收起开关（右上角）

| 状态 | 位置 | 图标 | 说明 |
| --- | --- | --- | --- |
| 收起（默认） | 会话右上角工具区 | **▣**（圆形图标，与左侧栏折叠按钮同款样式） | 点击展开侧栏 |
| 展开 | 仅出现在侧栏头部最右侧 | **▣** | 点击收起侧栏，会话区恢复宽度 |

> 展开状态下，右上角的开关会隐藏，收起图标只存在于侧栏面板中；收起后开关自动回到右上角。

### 3.2 侧栏面板（列表视图）

```
┌──────────────────────────────────────┐
│ [会话标题]              ⤢  ⟳  ▣      │ ← 头部（高度与聊天头部一致）
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │
│ │ 📄 文件名        📖 ⧉ ↗  大小 时间 │ │ ← 文件行（悬停显示操作按钮）
│ │ 📄 文件名        📖 ⧉ ↗  大小 时间 │ │
│ └──────────────────────────────────┘ │
│        （内容过多时整体垂直滚动）       │
└──────────────────────────────────────┘
```

#### 头部按钮（列表视图）

| 按钮 | 功能 |
| --- | --- |
| **⤢ / ⤡** | 全屏切换：面板占满整个屏幕宽度；再点还原 360px |
| **⟳** | 刷新产物列表 |
| **▣** | 收起侧栏（会话区恢复原始宽度） |

#### 文件行操作（悬停文件行后显示）

| 按钮 | 功能 | 说明 |
| --- | --- | --- |
| **📖** | 预览文件 | 进入**全屏内容模式**（只显示预览内容，列表隐藏） |
| **⧉** | 复制路径 | 复制该文件的**完整绝对路径**到剪贴板 |
| **↗** | 在文件夹中打开 | 在文件资源管理器中打开文件所在文件夹并**选中该文件** |

行尾同时显示文件**大小**和**修改时间**（相对时间，如「3 分钟前」）。

---

## 4. 预览模式（全屏内容）

点击文件行上的 **📖** 进入预览。预览是一个**独立的全屏内容视图**：

### 4.1 内容展示

- 进入预览后，**文件列表隐藏，只显示预览内容**（更聚焦）；
- 内容铺满侧栏主体区域，超出时**整体垂直滚动**；
- **文本文件**：显示内容（最多读取前 256KB，超长末尾提示「仅显示前 256KB」）；
- **二进制文件**（图片、压缩包、可执行文件等）：提示「二进制文件暂不支持预览」。

### 4.2 预览模式下的头部

```
┌──────────────────────────────────────────────┐
│ 📄 [文件名]         📂  ✕  ⤢  ▣            │
│     [相对路径/完整地址]                       │
└──────────────────────────────────────────────┘
```

| 按钮 | 功能 |
| --- | --- |
| **📂** | 在文件资源管理器中打开该产物所在文件夹 |
| **✕** | **关闭预览，返回产物文件列表** |
| **⤢ / ⤡** | 全屏切换 |
| **▣** | 收起侧栏 |

> 预览状态下头部会显示**当前预览文件的名称与路径**，方便定位。

---

## 5. 产物数据说明

- **数据来源**：从**当前会话的日志**中解析 `write`/`edit` 工具调用记录，提取该会话实际写过的文件；
- **会话归属**：只显示当前会话的产物，**切换会话时侧栏自动跟随**显示新会话的文件；
- **兜底机制**：若日志中没有 write/edit 记录，会扫描工作目录、按「会话开始之后修改过的文件」补充展示；
- **实时性**：面板打开后若会话产生了新文件，点击 **⟳** 刷新即可看到。

### 常见问题

| 问题 | 说明 |
| --- | --- |
| 显示「当前会话暂无产物文件」 | 该会话没有通过 write/edit 写入过文件，或文件已被删除 |
| 某些文件没有出现在列表 | 仅通过 bash 等命令直接创建、从未用 write/edit 工具写过的文件不会被记录（可在会话中要求用 write 工具写入后即会显示） |
| 预览显示「二进制文件暂不支持预览」 | 图片/压缩包/可执行文件等二进制内容，属于预期行为 |
| 预览内容是**当前磁盘内容** | 若文件在会话之后被修改过，预览显示的是最新内容而非写入时的快照 |
| 预览时看不到文件列表 | 属于设计行为：预览是全屏内容模式，点头部 **✕** 即可返回列表 |

---

## 6. 使用技巧

1. **大文件预览**：配合 **⤢ 全屏**，预览内容阅读更舒适；
2. **快速定位产物**：复制路径后可直接粘贴到终端/对话框使用；「在文件夹中打开」可直接在资源管理器中找到文件；
3. **多会话对比**：先在一个会话打开侧栏，再切换到其他会话，侧栏会随之切换，方便对照各会话的产物；
4. **恢复布局**：任何时候点 ▣ 收起，界面即恢复原始宽度。

---

## 7. 技术要点（可选了解）

- 侧栏通过官方 `layout.openDetails()/closeDetails()` 驱动详情列开合，实现**真正的布局嵌入**（会话区收窄/恢复）；
- 面板头部高度与聊天头部**动态测量对齐**（展开时测量聊天 `<header>` 的真实高度）；
- 图标与左侧栏折叠按钮同款（内联 SVG「面板」图标 + 圆形按钮）；
- **性能优化**：文件元数据检查并行执行 + 产物结果按会话缓存 8 秒（再次展开秒开，⟳ 刷新强制重算）；
- 全部样式跟随当前主题（浅色/深色/自定义配色自动适配）。

---

*本文档针对 `artf-2/pkg-21` 编写。若插件后续更新，功能以实际界面为准。*
