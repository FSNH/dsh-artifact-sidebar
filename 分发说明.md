# dsh-plugin-artifact-sidebar — 分发说明（给其他 DSH 用户）

> 部署级 DeepSeek Harness 插件：会话产物侧栏
> 发布渠道：npm（`dsh-plugin-artifact-sidebar`）· 源码：https://github.com/FSNH/dsh-artifact-sidebar

---

## 1. 这是什么

在 DSH Web GUI 页面右侧提供一个可展开/收起的**产物侧栏**，展示**当前会话**在对话中实际产生的文件，支持：

- 📌 右侧嵌入式侧栏（展开时会话区自动收窄 360px，收起恢复）
- 📂 当前会话产物（从会话日志提取，切换会话自动跟随）
- 📖 全屏内容预览（文本前 256KB，二进制自动提示，✕ 返回列表）
- ⧉ 复制路径 · ↗ 在文件夹中打开（Windows / macOS / Linux 均支持）
- ⤢ 全屏 · ⟳ 刷新 · ◧ 收起
- 🎨 主题跟随 · ⚡ 并行 stat + 8 秒缓存

---

## 2. 安装（部署管理员，约 1 分钟）

### ✨ 一键安装（推荐）

```bash
npx dsh-plugin-artifact-sidebar@latest install
```

自动完成：检测 `~/.dsh/profiles/*` → 写入 package.json 依赖 → 写入 `cordis.patch.yml` → `npm install`。幂等可重复运行。

卸载：

```bash
npx dsh-plugin-artifact-sidebar@latest uninstall
```

完成后**硬刷新页面**（Ctrl+F5，开发模式热加载）或**重启服务**（生产模式）。

### 手动安装（可选）

#### 2.1 添加依赖

编辑你的 DSH profile 的 `package.json`（通常位于 `~/.dsh/profiles/<profile>/package.json`）：

```json
"dependencies": {
  "dsh-plugin-artifact-sidebar": "^1.0.0"
}
```

然后安装依赖（在 profile 目录）：

```bash
npm install
```

### 2.2 注册插件

在 profile 的 `cordis.patch.yml` 追加：

```yaml
- insert:
    - id: artifact-sidebar
      name: dsh-plugin-artifact-sidebar
```

### 2.3 生效

- 开发模式：HMR 会自动热加载，**硬刷新页面**（Ctrl+F5）即可；
- 生产模式：重启 DSH 服务后刷新页面。

---

## 3. 使用

1. 进入任意会话 → 右上角出现圆形「面板」图标 ▣；
2. 点击展开 → 侧栏显示当前会话的产物文件；
3. 悬停文件 → 📖 预览 / ↗ 打开文件夹 / ⧉ 复制路径；
4. 点击 📖 → 全屏预览内容，头部 ✕ 返回列表；
5. 切换会话 → 侧栏自动跟随；
6. 点击 ▣ 收起 → 会话区恢复原宽。

插件会出现在 **设置 → 插件** 列表中，重启不丢失、全局生效。

---

## 4. 从 GitHub 源码安装（备选）

npm 尚未发布时，也可以直接从 GitHub 安装：

```json
"dependencies": {
  "dsh-plugin-artifact-sidebar": "github:FSNH/dsh-artifact-sidebar#main"
}
```

> 注意：需要仓库提供该包（`deployment-package/` 目录内容为包源码）。

---

## 5. 常见问题

| 问题 | 说明 |
| --- | --- |
| 刷新后侧栏没出现 | 确认 patch 行已加、依赖已装；开发模式需硬刷新，生产模式需重启 |
| 「打开文件夹」无反应 | 跨平台已支持（explorer / open -R / xdg-open）；若路径含特殊字符请反馈 |
| 预览显示「二进制文件暂不支持预览」 | 图片/压缩包等二进制内容，属于预期行为 |
| 想卸载 | 删除 `cordis.patch.yml` 中的行 + 移除 package.json 依赖，刷新/重启即可 |

---

## 6. 反馈

- GitHub Issues: https://github.com/FSNH/dsh-artifact-sidebar/issues
- 欢迎 Star / PR / 建议

*MIT License · 作者 FSNH*
